<header class="navbar navbar-expand-lg navbar-light bg-dark">
  <div class="container">
    <nav class='nav navbar-nav navbar-left' >
      <span>Bookers</span>
    </nav>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <ul class="nav-item"><%= link_to ' Home', user_path(current_user), class: 'glyphicon glyphicon-home home' %></ul>
          <ul class="nav-item"><%= link_to ' Users', users_path, class: 'glyphicon glyphicon-user users' %></ul>
          <ul class="nav-item"><%= link_to ' Books', books_path, class: 'glyphicon glyphicon-book books'%></ul>
          <ul class="nav-item"><%= link_to ' logout', destroy_user_session_path, method: :delete, class: 'glyphicon glyphicon-log-out logout' %></ul>
        <% else %>
          <ul class="nav-item"><%= link_to ' Home', root_path, class: 'glyphicon glyphicon-home home' %></ul>
          <ul class="nav-item"><%= link_to ' About', about_path, class: 'glyphicon glyphicon-link about' %></ul>
          <ul class="nav-item"><%= link_to ' sign up', new_user_registration_path, class: 'glyphicon glyphicon-edit sign_up' %></ul>
          <ul class="nav-item"><%= link_to ' login', new_user_session_path, class: 'glyphicon glyphicon-log-in sign_in'%></ul>
        <% end %>
      </ul>
    </nav>
  </div>
</header>